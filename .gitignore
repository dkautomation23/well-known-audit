node_modules/
dist/
*.tgz
# libFuzzer writes a reproducer next to the target when it finds something;
# the finding belongs in a test, not in the repository as a binary blob.
crash-*
leak-*
timeout-*
# The fuzzer's working corpus: 200+ machine-generated mutations that mean
# nothing to a reader. The five hand-written seeds live in fuzz/seeds/, and
# ClusterFuzzLite keeps its own corpus between runs.
fuzz/corpus/
